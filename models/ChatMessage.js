class ChatMessage {
    constructor(id, createdDate, message, user) {
        this.id = id;
        this.createdDate = createdDate;
        this.message = message;
        this.user = user;
    }
}

export default ChatMessage;